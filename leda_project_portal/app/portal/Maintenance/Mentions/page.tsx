import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Mentions"
}

export default function Page() {
    return (
        <main>
            <p>Mentions Page</p>
        </main>
    );
}