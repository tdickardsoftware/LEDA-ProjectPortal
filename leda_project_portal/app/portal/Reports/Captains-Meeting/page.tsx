import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Captains Meeting"
}

export default function Page() {
    return (
        <main>
            <p>Captains Meeting Page</p>
        </main>
    );
}