import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Save Points Letter"
}

export default function Page() {
    return (
        <main>
            <p>Save Points Letter Page</p>
        </main>
    );
}