import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Membership List"
}

export default function Page() {
    return (
        <main>
            <p>Membership List Page</p>
        </main>
    );
}