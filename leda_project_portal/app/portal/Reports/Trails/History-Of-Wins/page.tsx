import { Metadata } from "next";

export const metadata: Metadata = {
    title: "History of Wins"
}

export default function Page() {
    return (
        <main>
            <p>History of Wins Page</p>
        </main>
    );
}