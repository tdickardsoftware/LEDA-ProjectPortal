import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Reports"
}

export default function Page() {
    return (
        <main>
            <p>Reports Page</p>
        </main>
    );
}