import { Metadata } from "next";

export const metadata: Metadata = {
    title: "League Play"
}

export default function Page() {
    return (
        <main>
            <p>League Play Page</p>
        </main>
    );
}