import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Season Members"
}

export default function Page() {
    return (
        <main>
            <p>Season Members Page</p>
        </main>
    );
}