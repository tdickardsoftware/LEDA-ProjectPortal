import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Captains"
}

export default function Page() {
    return (
        <main>
            <p>Captains Page</p>
        </main>
    );
}