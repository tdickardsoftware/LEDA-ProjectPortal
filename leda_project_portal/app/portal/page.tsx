import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Portal | LEDA Project"
}

export default function Page() {
    return (
        <main>
            <p>Portal Page</p>
        </main>
    );
}