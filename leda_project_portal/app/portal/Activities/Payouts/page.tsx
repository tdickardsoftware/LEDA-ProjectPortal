import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Payouts"
}

export default function Page() {
    return (
        <main>
            <p>Payouts Page</p>
        </main>
    );
}