import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Activities"
}

export default function Page() {
    return (
        <main>
            <p>Activities Page</p>
        </main>
    );
}