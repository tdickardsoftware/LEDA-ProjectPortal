import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Rosters"
}

export default function Page() {
    return (
        <main>
            <p>Rosters Page</p>
        </main>
    );
}