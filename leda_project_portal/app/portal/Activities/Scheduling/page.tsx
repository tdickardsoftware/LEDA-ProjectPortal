import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Scheduling"
}

export default function Page() {
    return (
        <main>
            <p>Scheduling Page</p>
        </main>
    );
}