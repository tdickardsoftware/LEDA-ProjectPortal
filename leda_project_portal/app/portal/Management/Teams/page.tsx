import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Teams"
}

export default function Page() {
    return (
        <main>
            <p>Teams Page</p>
        </main>
    );
}