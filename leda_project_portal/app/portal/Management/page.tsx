import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Management"
}

export default function Page() {
    return (
        <main>
            <p>Management Page</p>
        </main>
    );
}