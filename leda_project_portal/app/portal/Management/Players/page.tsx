import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Players"
}

export default function Page() {
    return (
        <main>
            <p>Players Page</p>
        </main>
    );
}