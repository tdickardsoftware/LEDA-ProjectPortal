import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Players Not Paid"
}

export default function Page() {
    return (
        <main>
            <p>Players Not Paid Page</p>
        </main>
    );
}