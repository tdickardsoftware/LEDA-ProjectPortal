import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Players No Form"
}

export default function Page() {
    return (
        <main>
            <p>Players No Form Page</p>
        </main>
    );
}