import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Mailing Labels"
}

export default function Page() {
    return (
        <main>
            <p>Mailing Labels Page</p>
        </main>
    );
}