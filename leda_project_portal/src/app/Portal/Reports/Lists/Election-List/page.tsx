import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Election List"
}

export default function Page() {
    return (
        <main>
            <p>Election List Page</p>
        </main>
    );
}