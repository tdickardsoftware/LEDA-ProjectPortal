import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Lists"
}

export default function Page() {
    return (
        <main>
            <p>Lists Page</p>
        </main>
    );
}