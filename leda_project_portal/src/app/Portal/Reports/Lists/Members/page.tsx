import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Members"
}

export default function Page() {
    return (
        <main>
            <p>Members Page</p>
        </main>
    );
}