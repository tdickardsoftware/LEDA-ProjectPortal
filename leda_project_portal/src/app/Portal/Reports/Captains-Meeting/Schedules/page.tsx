import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Schedules"
}

export default function Page() {
    return (
        <main>
            <p>Schedules Page</p>
        </main>
    );
}