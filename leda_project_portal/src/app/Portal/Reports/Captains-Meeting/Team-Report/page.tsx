import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Team Report"
}

export default function Page() {
    return (
        <main>
            <p>Team Report Page</p>
        </main>
    );
}