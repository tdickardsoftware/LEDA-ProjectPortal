import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Folder Labels"
}

export default function Page() {
    return (
        <main>
            <p>Folder Labels Page</p>
        </main>
    );
}