import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Points List"
}

export default function Page() {
    return (
        <main>
            <p>Points List Page</p>
        </main>
    );
}