import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Trails"
}

export default function Page() {
    return (
        <main>
            <p>Trails Page</p>
        </main>
    );
}