import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Eligible for Trip"
}

export default function Page() {
    return (
        <main>
            <p>Eligible for Trip Page</p>
        </main>
    );
}