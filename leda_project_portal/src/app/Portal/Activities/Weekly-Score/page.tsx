import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Weekly Score"
}

export default function Page() {
    return (
        <main>
            <p>Weekly Score Page</p>
        </main>
    );
}