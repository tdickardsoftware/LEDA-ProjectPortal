import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Places"
}

export default function Page() {
    return (
        <main>
            <p>Places Page</p>
        </main>
    );
}