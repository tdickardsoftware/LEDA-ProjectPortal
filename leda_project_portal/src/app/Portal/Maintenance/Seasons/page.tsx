import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Seasons"
}

export default function Page() {
    return (
        <main>
            <p>Seasons Page Page</p>
        </main>
    );
}