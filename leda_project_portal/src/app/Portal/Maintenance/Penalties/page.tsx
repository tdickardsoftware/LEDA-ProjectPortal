import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Penalties"
}

export default function Page() {
    return (
        <main>
            <p>Penalties Page</p>
        </main>
    );
}