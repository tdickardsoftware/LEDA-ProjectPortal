import { Metadata } from "next";

export const metadata: Metadata = {
    title: "People Types"
}

export default function Page() {
    return (
        <main>
            <p>People Types Page</p>
        </main>
    );
}