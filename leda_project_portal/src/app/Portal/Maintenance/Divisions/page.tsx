import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Divisions"
}

export default function Page() {
    return (
        <main>
            <p>Divisions Page</p>
        </main>
    );
}