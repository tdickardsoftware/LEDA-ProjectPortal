import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Place Types"
}

export default function Page() {
    return (
        <main>
            <p>Place Types Page</p>
        </main>
    );
}